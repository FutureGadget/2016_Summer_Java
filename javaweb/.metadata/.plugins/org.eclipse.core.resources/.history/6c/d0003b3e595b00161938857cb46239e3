package service;

import dao.UserDAO;
import entity.User;

public class UserService {
	public boolean authenticate(User user) {
		UserDAO dao = new UserDAO();
		if (user.getUpw().equals(dao.getPassword(user)) {
			
		}
	}
}
